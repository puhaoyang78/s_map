# =====================================================================
# Frontend Vite environment template
# Copy to front/.env.local for local development.
# =====================================================================

# Development uses the Vite /api proxy by default.
# Set VITE_API_BASE_URL only when you intentionally want a non-proxied API origin.
VITE_MAPBOX_TOKEN=pk.replace-with-your-public-mapbox-token
